<body>
    <h1>Dev Skills</h1>
    <ul>
        <% skills.forEach(function(t) { %>
            <li><a href="/skills/<%= t.id %>">
                    <%= t.skill %>
                </a> - <%= t.advanced ? 'ADVANCED' : 'NOT ADVANCED' %>
            </li>
            <% }); %>
    </ul>
</body>